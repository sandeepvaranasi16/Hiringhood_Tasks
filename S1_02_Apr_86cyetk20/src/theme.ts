import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#6C63FF", dark: "#3F3BFF" },
    secondary: { main: "#dc004e" },
  },
  typography: {
    fontFamily: "Kanit",
  },
});

export default theme;
